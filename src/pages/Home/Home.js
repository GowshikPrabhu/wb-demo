import React from "react";
import { BottomSheet, Header } from "../Components";

const Home = () => {
  return (
    <div>
      <Header />
      <BottomSheet />
    </div>
  );
};

export default Home;
